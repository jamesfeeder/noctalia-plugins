# Issue tracker

Issues and PRDs live as Markdown under root `.scratch/`.

- One feature per `.scratch/<feature>/` directory.
- Put its PRD at `PRD.md`.
- Put numbered implementation issues under `issues/`.
- Record triage state with a `Status:` line.
- Name the affected plugin in each issue; use `repository` for shared tooling.

