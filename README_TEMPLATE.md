# Plugin Name

Short explanation of what the plugin does.

## Plugin

| Field | Value |
| --- | --- |
| id | `jamesfeeder/plugin-name` |
| version | `0.1.0` |
| plugin API | `3` |

## Usage

Explain how to enable and access every declared entry. Include exact widget
types, panel toggle commands, shortcut ids, or launcher prefixes as applicable.

## Settings

Document every declared setting, including units and non-obvious behavior.
Remove this section when the plugin has no settings.

## Requirements

List every manifest dependency by its exact name. Write "None" when empty.

## Notes

Document filesystem access, network calls, spawned processes, compositor
behavior, privacy implications, or other operational details.

## TODO

- Add optional 960×540 `thumbnail.webp` when useful.
- Add CI coverage when repository CI is introduced.

