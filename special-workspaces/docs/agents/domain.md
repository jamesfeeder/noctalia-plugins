# Domain documentation

This repository has one domain context.

- Read the root `CONTEXT.md` before changing behavior.
- Use its glossary terms consistently.
- Read relevant decisions under `docs/adr/` when that directory exists.
- Surface conflicts with an existing ADR instead of silently overriding it.
